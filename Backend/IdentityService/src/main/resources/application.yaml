server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/Identity_service}
    username: ${DBMS_USERNAME:root}
    password: ${DBMS_PASSWORD:root}
   # driverClassName: "com.mysql.cj.jdbc.Driver"
  kafka:
    bootstrap-servers: localhost:9094 #server kafka
    producer: #config cho producer (nguoi gui)
      key-serializer: org.apache.kafka.common.serialization.StringSerializer #su dung type string
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer #su dung json de gui object
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
outbound:
  identity:
    client-id: "948254557534-ca906iu2s6s1a33hcuvhv1pdhp9qmoti.apps.googleusercontent.com"
    client-secret: "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"
    redirect-url: "http://localhost:3000/Authenticate"
    grant-type: "authorization_code"



jwt:
  signerKey: "POYgGbpnFvfHTnSkIF/UF0ZO4OMa8ewTfjNGP1+rKmBZB5bPnDczsiDxERPVu2pn"
  valid-duration: 3600 # in second
  refreshable-duration: 360000 #second

app:
  services:
    profile: "http://localhost:8081/profile"



